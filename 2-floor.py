#!/usr/bin/env python3
"""A function that returns the floor of a float, with type annotations."""
import math


def floor(n: float) -> int:
    """Return the floor of n as an int."""
    return math.floor(n)
